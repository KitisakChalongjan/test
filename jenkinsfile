pipeline {
    agent { 
        dockerfile true 
    }

    stages {
        stage('Build Golang API') {
            steps {
                echo 'Building...'
                sh 'go version'
                sh 'docker build -t test-image .'
            }
        }
    }
}