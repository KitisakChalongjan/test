pipeline {
    agent { 
        dockerfile true 
    }

    stages {
        stage('Build Service Image') {
            steps {
                echo 'Building...'
                sh 'go version'
                sh 'docker build -t test-image .'
            }
        }
    }
}